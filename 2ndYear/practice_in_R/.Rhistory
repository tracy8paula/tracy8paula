discount_tree <- rpart(`Discount (Yes/No)` ~ ., data = train_data, method = "class")
##building our decision tree based on discount
install.packages("rpart")
library(rpart)
discount_tree <- rpart(`Discount (Yes/No)` ~ ., data = train_data, method = "class")
## assignment 3
#no. 1 Importing data set and renaming
install.packages("readxl")
library(readxl)
Assignment3_Adongo <- read_excel("C:/Users/EXTON TECH/Desktop/Introduction to Data Science/Vegetable_Sales-1.xlsx")
View(Assignment3_Adongo)
#no. 2 Highest selling vegetable
max(Assignment3_Adongo$`Quantity Sold (kg)`, na.rm = TRUE)
#no. 3 Number of vegetables sold on discount
times <- gregexpr("yes", Assignment3_Adongo$`Discount (Yes/No)`, ignore.case = TRUE)
time_list <- regmatches(Assignment3_Adongo$`Discount (Yes/No)`, times)
num <- length(unlist(time_list))
print(num)
#no. 4 Descriptive statistics of the quantity of items sold
summary(Assignment3_Adongo$`Quantity Sold (kg)`)
#no. 5 Which vegetable should be removed
max(Assignment3_Adongo$`Loss Rate (%)`, na.rm = TRUE)
#no. 7
# Machine learning model to predict whether potato is sold under discount or not
data <- read_excel("C:/Users/EXTON TECH/Desktop/Introduction to Data Science/Vegetable_Sales-1.xlsx")
View(data)
## splitting data
set.seed(30)
## splitting data
set.seed(20)
#splitting by 70% to train the model
train = sample(1:nrow(data), nrow(data)*0.7)
# viewing data to train
train_data <- data[train, ]
str(train_data)
# our new data frame has 176 observations which is 70% of our original data set
## splitting by 30% to test the model
test_data <- data[-train, ]
str(test_data)
##building our decision tree based on discount
install.packages("rpart")
library(rpart)
discount_tree <- rpart(`Discount (Yes/No)` ~ ., data = train_data, method = "class")
## plotting our decision tree
install.packages("rpart.plot")
library(rpart.plot)
rpart.plot(discount_tree, main= "Decision tree for vegetables on discount")
#To view dataset
View(Data1)
#######Installing Packages For Usage#####
#Package for importing excel data file
install.packages("readxl")
#Once package is installed, you need to call up the library to use the codes in it
library(readxl)
#Code for importing
Data1 <- read_excel("C:/Users/EXTON TECH/Desktop/2ndYear/practice_in_R/Bike_Sales.xlsx")
#To view dataset
View(Data1)
#To view dataset
View(Data1)
#START BY SETTING UP YOUR WORKING ENVIRONMENT #
setwd("C:/Users/EXTON TECH/Desktop/2ndYear/practice_in_R")
#To check the working directory
getwd()
#To generate a new dataframe
Name = c("Cathy", "Humble", "Daniel", "Anitah")
Weight = c(40, 52, 78, 101)
#Combine above variables of  name and weight to make a dataframe
# called "class_weight"
class_weight = data.frame(Name, Weight)
## Calculating the central tendency dataframe ##
summary(class_weight)
#The result of a mean = 67.75, median = 65.0 shows the weight is not normally distributed
mean(class_weight)
#The above code gives an error because the variable Name is qualitative
mean(class_weight$Weight)
#Visually plotting the central tendency
boxplot(class_weight$Weight, main="Class Weight", boxwex=0.1)
#Central tendency of Customer_Age in the Bike_Sales dataset
boxplot(Data1$Customer_Age, main="Ages of Customers Buying Bicycle", boxwex=0.1)
#Looking for cental tendency stats
summary(Data1$Customer_Age)
#Display the variable visually using a histogram
hist(class_weight$Weight, main = "Class weight using a histogram")
